# 🚀 Telegram MTProxy Auto Installer (Docker)

![Docker](https://img.shields.io/badge/Docker-2496ED?style=for-the-badge&logo=docker&logoColor=white)
![Shell Script](https://img.shields.io/badge/Shell_Script-121011?style=for-the-badge&logo=gnu-bash&logoColor=white)
![Telegram](https://img.shields.io/badge/Telegram-2CA5E0?style=for-the-badge&logo=telegram&logoColor=white)
![License](https://img.shields.io/badge/License-MIT-green?style=for-the-badge)

Автоматический скрипт для быстрого развертывания **MTProxy** на вашем VDS/VPS сервере с использованием **Docker**.

Скрипт использует **официальный образ** `telegrammessenger/proxy`, что гарантирует стабильность, безопасность и отсутствие проблем с устаревшими репозиториями.

---

## ✨ Особенности

* 🛠 **Полная автоматизация:** Устанавливает Docker (если его нет), генерирует секреты и запускает контейнер.
* 🔒 **Безопасность:** Использует официальный и проверенный Docker-образ от Telegram.
* ⚡ **Производительность:** Работает быстро и потребляет минимум ресурсов.
* 🔑 **Удобство:** В конце установки выводит готовую `tg://` ссылку для подключения.

## 📋 Требования

* Сервер (VPS/VDS) с ОС **Ubuntu** или **Debian**.
* Права **root** (или возможность использования `sudo`).
* Открытый порт (по умолчанию `8443`, можно изменить в скрипте).

## 🚀 Быстрый старт

Выполните следующие команды на вашем сервере:

```bash
# 1. Клонируем репозиторий
git clone https://github.com/Reibik/st-village-proxy.git

# 2. Переходим в папку
cd st-village-proxy

# 3. Делаем скрипт исполняемым
chmod +x install_proxy.sh

# 4. Запускаем установку
sudo ./install_proxy.sh

```

## ⚙️ Настройка

По умолчанию скрипт использует порт `8443`. Если вы хотите изменить порт, откройте файл `install_proxy.sh` любым текстовым редактором и измените переменную `PORT` в начале файла:

```bash
# Порт на сервере (внешний)
PORT=8443

```

## 🛠 Управление прокси

**Просмотр логов (статус работы):**

```bash
docker logs -f mtproto-proxy

```

**Остановка и удаление прокси:**

```bash
docker rm -f mtproto-proxy

```

**Перезапуск (например, после перезагрузки сервера):**
Контейнер настроен с флагом `--restart=always`, поэтому он автоматически запустится после перезагрузки сервера. Ручной перезапуск не требуется.

## ❓ Решение проблем

**Не удается подключиться?**

1. Проверьте, запущен ли Docker контейнер: `docker ps`.
2. Убедитесь, что порт открыт в брандмауэре (Firewall):
```bash
# Для UFW (Ubuntu)
sudo ufw allow 8443/tcp

```



---

**Disclaimer:** Этот скрипт предназначен для образовательных целей и помощи в настройке личного прокси-сервера. Используйте его в соответствии с законами вашей страны.
